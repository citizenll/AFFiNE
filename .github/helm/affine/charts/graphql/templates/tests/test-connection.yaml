apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "affine-graphql.fullname" . }}-test-connection"
  labels:
    {{- include "affine-graphql.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "affine-graphql.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
