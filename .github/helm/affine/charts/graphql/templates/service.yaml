apiVersion: v1
kind: Service
metadata:
  name: {{ include "affine-graphql.fullname" . }}
  labels:
    {{- include "affine-graphql.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "affine-graphql.selectorLabels" . | nindent 4 }}
